<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.ctx.service.mapper.slave1.UserInfoMapper">
	<select id="selectByOddUserId" resultType="Map">
		select
			*
		from test2
	</select>
	
	<insert id="addSlaveData">
		insert into test2(remarks,emoji) values(now(),#{emoji})
	</insert>
</mapper>